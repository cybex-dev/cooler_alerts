enum CoolAlertType {
  success,
  error,
  warning,
  confirm,
  info,
  loading,
  custom,
}

enum CoolAlertAnimType {
  scale,
  rotate,
  slideInDown,
  slideInUp,
  slideInLeft,
  slideInRight,
}
